# Public site URL (canonical links, Open Graph, sitemap, robots.txt)
VITE_SITE_URL=https://your-domain.vercel.app

# Optional: only required when frontend and backend are on different domains.
# Leave empty when deploying as a single Vercel project (same-origin /api rewrites).
# VITE_API_URL=https://your-backend.vercel.app
