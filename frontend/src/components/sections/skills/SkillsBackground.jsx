import HeroBackground from '../hero/background/HeroBackground'

/** Reuses the exact Hero living background for visual continuity. */
export default function SkillsBackground({ sectionRef }) {
  return <HeroBackground sectionRef={sectionRef} />
}
